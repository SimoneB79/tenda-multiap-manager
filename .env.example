# Password for all Tenda APs (required)
TENDA_PASSWORD=

# MQTT integration (optional — leave empty to disable)
MQTT_BROKER=
MQTT_USER=
MQTT_PASSWORD=
POLL_INTERVAL=120

# Server port (default: 3000)
PORT=3000
